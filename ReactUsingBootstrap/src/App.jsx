import { Button } from "react-bootstrap";
import { Alert } from "react-bootstrap";
function App() {
  return (
    <>
      <h1>Add Bootstrap in React JS</h1>
      <Button>OK</Button>
      <Alert variant="primary">Hello, BT installed..!</Alert>
      <Alert variant="secondary">Hello, BT installed..!</Alert>
      <Alert variant="danger">Hello, BT installed..!</Alert>
      <Alert variant="info">Hello, BT installed..!</Alert>
      <Alert variant="warning">Hello, BT installed..!</Alert>
      <Alert variant="success">Hello, BT installed..!</Alert>
      <Alert variant="dark">Hello, BT installed..!</Alert>
      <Alert variant="light">Hello, BT installed..!</Alert>
    </>
  );
}

export default App;
